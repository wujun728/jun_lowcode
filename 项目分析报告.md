# jun_lowcode_amis_v2 项目分析报告

## 项目概述
jun_lowcode_amis_v2 是一个基于 Spring Boot + magic-api + amis + logic-flow 的低代码系统，支持实时生成 REST API 并热加载，提供动态执行 Java/Groovy/SQL 脚本、动态注册 RequestMapping 等功能。

## 项目结构

### 核心目录结构
```
jun_lowcode/
├── jun_lowcode_amis_v2/      # 主项目目录
│   ├── src/main/java/        # Java 源代码
│   │   ├── com.jqp/          # 项目入口
│   │   └── io.github.wujun728/ # 核心代码
│   ├── src/main/resources/   # 资源文件
│   │   ├── application.properties # 配置文件
│   │   ├── static/           # 静态资源
│   │   └── ui-json-template/ # UI 模板
│   └── pom.xml               # Maven 配置
└── jun_db/                   # 数据库相关
```

### 核心模块划分

| 模块 | 主要职责 | 关键类/接口 |
|------|---------|------------|
| admin.page | 页面和表单管理 | PageController, FormAmisController |
| admin.common | 通用功能 | CommonAmisController |
| admin.db | 数据库管理 | 数据库操作相关类 |
| admin.rbac | 权限管理 | 用户、角色、权限控制 |
| groovy | 动态脚本执行 | GroovyApiService, GroovyScriptController |
| record | 数据记录处理 | 数据模型和通用服务 |
| rest | REST API 支持 | RestApiController, RestSqlController |
| sql | SQL 相关功能 | ApiEngine, SqlWithParam |

## 核心功能点

### 1. 页面设计与管理
- **功能描述**：通过可视化界面设计数据展示页面，支持列表、查询条件、按钮等配置
- **核心实现**：
  - `PageController`：处理页面的增删改查请求
  - `PageServiceImpl`：实现页面的业务逻辑，包括数据加载、保存、查询等
  - `pageEdit.html`：页面设计的前端界面

### 2. 表单设计与管理
- **功能描述**：通过可视化界面设计表单，支持各种表单控件的配置
- **核心实现**：
  - `FormAmisController`：处理表单的增删改查请求
  - `FormServiceImpl`：实现表单的业务逻辑
  - `formEdit.html`：表单设计的前端界面

### 3. 动态 API 生成
- **功能描述**：基于 magic-api 实现动态 API 生成和热加载
- **核心实现**：
  - `magic-api.web`：magic-api 的 Web 入口配置
  - `ApiConfigCache`：API 配置缓存
  - `GroovyApiService`：Groovy 脚本 API 服务

### 4. 权限管理
- **功能描述**：基于 RBAC 模型实现用户、角色、权限的管理
- **核心实现**：
  - `rbac.controller`：权限相关的控制器
  - `rbac.service`：权限相关的业务逻辑
  - `TokenUtil`：Token 生成和验证

### 5. 数据库操作
- **功能描述**：提供动态 SQL 执行和数据库操作能力
- **核心实现**：
  - `JdbcService`：数据库操作服务
  - `ApiEngine`：SQL API 引擎
  - `SqlWithParam`：带参数的 SQL 对象

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | - | 后端框架 |
| magic-api | - | 动态 API 框架 |
| amis | - | 前端低代码框架 |
| Groovy | - | 动态脚本语言 |
| MySQL | - | 数据库 |
| Thymeleaf | - | 模板引擎 |
| Hutool | - | Java 工具包 |

## 关键类与接口

### 1. PageController
- **职责**：处理页面管理的 HTTP 请求
- **主要方法**：
  - `query`：查询页面列表
  - `get`：获取页面详情
  - `save`：保存页面配置
  - `js`：生成页面的 JavaScript 代码

### 2. FormAmisController
- **职责**：处理表单管理的 HTTP 请求
- **主要方法**：
  - `query`：查询表单列表
  - `get`：获取表单详情
  - `save`：保存表单配置
  - `js`：生成表单的 JavaScript 代码

### 3. JdbcService
- **职责**：提供数据库操作服务
- **主要方法**：
  - `query`：执行查询操作
  - `saveOrUpdate`：保存或更新数据
  - `delete`：删除数据

### 4. PageService
- **职责**：页面业务逻辑服务
- **主要方法**：
  - `get`：获取页面信息
  - `save`：保存页面配置
  - `query`：执行页面查询

### 5. FormService
- **职责**：表单业务逻辑服务
- **主要方法**：
  - `get`：获取表单信息
  - `save`：保存表单配置
  - `getFormJson`：生成表单的 JSON 配置

## 配置文件

### 主要配置文件
- `application.properties`：项目主配置文件
- `application-dev.properties`：开发环境配置
- `magic-editor-config.js`：magic-api 编辑器配置

### 关键配置项
- `spring.datasource.url`：数据库连接 URL
- `magic-api.web`：magic-api 的 Web 入口
- `magic-api.resource.type`：API 资源存储类型
- `server.port`：服务器端口

## 前端界面

### 主要页面
- `pageEdit.html`：页面设计器
- `formEdit.html`：表单设计器
- `onlineEdit.html`：在线编辑器
- `tableEdit.html`：表格编辑器

### 前端技术
- Vue.js 3.2.36
- Element Plus 2.2.28
- jQuery
- amis 低代码框架

## 系统流程

### 1. 页面设计与生成流程
1. 用户在 `pageEdit.html` 设计页面
2. 保存页面配置到数据库
3. 通过 `PageController.js` 接口生成页面的 JavaScript 代码
4. 前端加载生成的 JavaScript 代码，渲染页面

### 2. API 执行流程
1. 用户请求 API
2. magic-api 解析 API 配置
3. 执行对应的 SQL 或 Groovy 脚本
4. 返回执行结果

## 项目特点

1. **低代码设计**：通过可视化界面设计页面和表单，减少代码编写
2. **动态 API**：基于 magic-api 实现动态 API 生成和热加载
3. **灵活扩展**：支持 Groovy 脚本扩展业务逻辑
4. **权限控制**：基于 RBAC 模型实现细粒度的权限控制
5. **模板化生成**：使用模板引擎生成前端代码，提高开发效率

## 总结
jun_lowcode_amis_v2 是一个功能完整的低代码开发平台，通过可视化设计和动态 API 生成，大幅提高了开发效率。项目采用模块化设计，结构清晰，易于扩展和维护。